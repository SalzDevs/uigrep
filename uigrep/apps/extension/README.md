# uigrep browser companion

The WXT extension stays dormant until the desktop shortcut or browser command activates capture mode. The page UI is rendered in an isolated Shadow DOM and supports click selection, drag selection, multiple numbered regions, attached comments, and one atomic send.

During development, copy the local token generated by the desktop app into the extension options page. Installer-managed pairing replaces this step before release.
