{
  "name": "@uigrep/mcp-server",
  "version": "0.0.1",
  "description": "Local MCP server — coding agents pull uigrep feedback through it",
  "type": "module",
  "main": "./src/main.ts",
  "bin": { "uigrep-mcp": "./src/main.ts" },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc --noEmit",
    "start": "node src/main.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@uigrep/schema": "workspace:*",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
